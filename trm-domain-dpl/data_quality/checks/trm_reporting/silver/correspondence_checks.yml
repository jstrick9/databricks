# /data_quality/checks/trm_reporting/silver/correspondence_checks.yml
checks:
  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: ser_num

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: cor_nm

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: add_line1

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: city_nm

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: ctry_cd

  - criticality: warn
    check:
      function: valid_iso_country_code
      arguments:
        col_name: ctry_cd

  - criticality: warn
    check:
      function: valid_us_postal_address

  - criticality: warn
    check:
      function: valid_email_format
      arguments:
        col_name: cr_email1

  - criticality: warn
    check:
      function: electronic_correspondence_authorized