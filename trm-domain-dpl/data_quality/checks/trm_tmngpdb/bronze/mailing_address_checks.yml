# /data_quality/checks/trm_tmngpdb/bronze/mailing_address_checks.yml
checks:
  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: mailing_address_gid

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: name_line_1_tx

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: street_line_1_tx

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: country_cd

  - criticality: warn
    check:
      function: is_not_null
      arguments:
        column: address_type_ct 

  - criticality: warn
    check:
      function: is_unique
      arguments:
        columns: [mailing_address_gid]
        nulls_distinct: true

  - criticality: warn
    check:
      function: valid_us_domestic_address

  - criticality: warn
    check:
      function: no_placeholder_name

  - criticality: warn
    check:
      function: valid_international_postal_code

  - criticality: warn
    check:
      function: valid_address_type

  - criticality: warn
    check:
      function: created_before_last_modified
      arguments:
        create_col: create_ts
        modified_col: last_mod_ts